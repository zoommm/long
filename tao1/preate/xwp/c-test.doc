1. 定义一个字符串char str[] = "abc"，以下哪些方法可以打印str的内容？

A. printf("%c", str);
B. printf(str);str把地址传给了printf,prinrf当成%s来执行！
C. printf("%d", str);
D. printf("%s", str);
(d)BD
2. 语句块
for(i=0; i<10; i++) {
    ....
}
和语句块
i=0
while(i<10) {
    ....
    i++;
}
是不是完全等价的？
当两个循环遇到contine得时候，就不一样了！，for执行i++，while，直接判断i<10；
A. 不是
B. 是
(b)A
3. 以下哪些语句能够正确打印出helloworld？

A. printf("hello/*comment */world");
B. printf("%s",/*comment*/"helloworld");
C. printf("hello "/*comment*/"world");这个中间有空格，要求得是不代空格得，考得是严谨性得，打印得不是hello world。
D. printf("hello" /* hello */
       "world" /* world */
      );
BD
编译器在编译得时候，把空格，换行，过滤掉！
4. 函数定义void foo(int) {...}和foo(int) {...}意思相同吗？

A. 相同
B. 不同
(a)B
不同得编译其得，不定义的华，编译器，不同，处理得不同！
5. 用==比较字符'\0'和数字0，它们相等吗？

A. 不相等
B. 相等
(b)
他们都用2进制来表示，是相同得
6. 以下哪些语法元素的命名遵守标识符的命名规则？

A. 宏定义
B. 语句标号：一般和goto使用。标号用：来定义。如string：就是个。
C. 头文件名中.h之前的部分
D. 枚举成员
E. 结构体Tag
ABDE
标识符，起到一个标识得作用。
7. 在gdb中查看当前设置了哪些断点用什么命令？

A. display breakpoints
B. show breakpoints
C. info breakpoints
D. list breakpoints
C

8. 已知x = 43,ch = 'A',y=0，那么表达式x>=y&&ch<'B'&&!y的值等于

A. 布尔常量true
B. 语法错
C. 0
D. 布尔常量false
E. 1
(e)
布尔常量：c但中没有布尔，c89，没有布尔，c99引入了布尔定义。c++中有布尔。
9. 以下标识符，哪些是不合法的？
(b,c）
A. IF
B. #u
C. 9you
D. void：关键字，不可以。
E. _abc_
AE合法，BCD不合法。
10. 下面表达式中能够判断字符变量ch是否为大写字母的是

A. (ch-'A'>=0)&&('Z'-ch>=0)
B. 'A'<=ch<='Z'
C. (ch>='A')&(ch<='Z')
D. (ch-'A'>0)&&(ch-'Z'<0)
(b)A
11. 如果使用printf("参数");打印以下内容，则参数部分应怎么写？
Tim says\, "The discount is 10%."

_______________________
printf("Tim says\'\',\"\The discount is 10\%\"\");
printf("Tim says\\,\"The discount is 10%%.\"");

12. 以下哪些是合法的语句？

A. 3<0;
B. for(;;);
C. 1;
D. while();
(c)ACB
13. 表达式a<b||!c!=2&&d中的运算符按优先级从高到低的顺序是

_______________________
(!),(!=),(<),(||),(&&)
! < != ||  &&
~ 取反也是单目运算符。
14. 定义结构体
struct sometype {
    int a[3];
};
则以下哪些初始化语句是不正确的？

A. struct sometype sth = {{}};
B. struct sometype sth = {};
C. struct sometype sth = {3,2,};
D. struct sometype sth = {{3,2}};
E. struct sometype sth = {3,,2};
(bce)E

15. 正确表述"x取值在[1，10]和[200,210]之内为真，其它为假"的表达式有哪些？

A. (x>=1)||(x<=10)||(x>=200)||(x<=210)
B. (x>=1)||(x<=10)&&(x>=200)||(x<=210)
C. (x>=1)&&(x<=10)||(x>=200)&&(x<=210)
D. (x>=1)&&(x<=10)&&(x>=200)&&(x<=210)
B

16. 在函数foo中调用main函数会怎么样？

A. 可能会出运行时错误
B. 可能会正常运行
C. 一定会出运行时错误
D. 编译错误
AB

17. 以下这种注释的写法正确吗？
/*
 * This is a surrounding comment
 * // a former comment
 */

A. 正确
B. 不正确
(a)A
18. 定义两个结构体struct { int member[10]; } a, b; 能用a = b把b的内容拷贝给a吗？

A. 能
B. 不能
(A)
结构体可以相互复值。数组不可以直接用=号赋值。

19. 定义 int k = 7, x = 12;找出结果为3的表达式

A. x=x%k-k%5
B. x=x%(k-k%5)
C. k=(x%k)-(k%5)
D. x=x%(k=k%5)
BD

20. 选出下列选项中书写正确的字符常量。

A. "c"：  字符串，
B. '\\'： \是个转译字符。
C. '%%'： 用printf（ %%）得时候能打印一个%，但是字符得时候%%不可以，用一个%就可以了。
D. '''： 出现奇异，不知道用哪个‘号。单引号，拴引号，反斜杠，都需要转译～～！
E. '"'：
EB

21. 定义两个数组int a[10]; int b[10]; 能用a = b把数组b的内容拷贝给a吗？

A. 能
B. 不能
B
22. 关于逻辑运算符&&和||两侧操作数的类型，以下哪些说法正确？

A. 只能是整型或字符型数据：void也可以，但是不是整型和字符。
B. 可以是任何类型的数据：float不可以。
C. 只能是0或非零的正数：负数也可以，负数是整形得一种。
D. 只能是1或0：
E. ||和&&把两侧操作数都当作0和1进行逻辑运算
E

23. 假设a、b、c、d都是double型变量，以下数学分数形式在C语言中可以怎样表示？
3ad
----
 bc
3ad处以bc，以下形式：
A. 3*a*d/(b*c)
B. 3*a*d/b*c
C. (3*a*d)/b*c
D. 3*a*d/b/c
AD

24. 以下代码段的输出结果是什么？
if (a=b)
 printf("a=b")
else
 printf("a!=b");

A. a=b 
B. 编译会出错
C. a!=b  
D. 不一定  
E. 运行会出错
D
未初始得全局变量在bss断，数值为0；
malloc 也给初始话成0；
25. 为什么++(a+1)这样的表达式不能通过编译而++a[1]却可以？

_______________________
?
a[1]可以作左值，因为是数组，合法得。A+1就不能作左值，因为是表达式，不合法。
数组明不能进行，自加和自减操作。要想自加，定义一个指针，只想它，自己加。
26. 已知字母A的ASCII码为十进制数65，c2为字符型，执行c2='A'+'6'-'3'后，c2的值为

A. 68
B. 不确定
C. 'D'
D. 'C'
E. 67
CA
27. 可以这样定义全局变量吗？
int i=0;
int j=i+1;

A. 不可以
B. 可以

A

28. 如果数组访问越界可能会引起：

A. 语义错误：语义错误，是在编译得时候运作得，编译得时候编译器不知道！
B. 编译错误：null，把地址--，
C. 段错误：
D. 运行时错误：
E. 可能什么错误都没有，一切正常：
CADE

29. 函数定义int foo(void) {...}和foo() {...}意思相同吗？

A. 不同
B. 相同
A

30. foo函数里的goto语句可以跳转到main函数的某个标号吗？
反过来，main函数里的goto语句可以跳转到foo函数的某个标号吗？

A. 不可以，不可以
B. 可以，不可以
C. 不可以，可以
D. 可以，可以

A
goto得作用范围只限于本函数得体内！

==========================
Answer
==========================
1. B D

2. A

3. B D

4. B

5. B

6. A B D E

7. C

8. E

9. B C D

10. A

11. Tim says\\, \"The discount is 10%%.\"

12. A B C

13. !, <, !=, &&, ||

14. E

15. C

16. A B

17. A

18. A

19. A C

20. B E

21. B

22. E

23. A D

24. D

25. a[1]可以做左值，因此++a[1]是合法的，而a+1不能做左值，因此++(a+1)是不合法的

26. A C

27. A

28. A C D E

29. A

30. A

上机考试题（开卷，满分100分）

1、（40分）编程实现一个猜数字游戏。游戏规则：首先程序生成一个随机数（以下称为题目），由4位不重复的数字组成，如1234、7603等，然后提示玩家输入4位不重复的数字，程序会告知玩家所猜答案与题目是否相符，如果不相符，程序会以A、B标识提示玩家，A表示数字和位置都正确，B表示数字正确位置不正确。例如程序出的题目是1234，则运行过程可以是这样：

玩家输入：8765（没有一个数字正确）
程序提示：0000
玩家再猜：5283（猜对了2和3，但是3的位置错了）
程序提示：AB
玩家再猜：3261（猜对了3、2和1，但是3、1的位置错了）
程序提示：ABB
玩家输入：I GIVE UP       或者玩家猜中：1234
则程序给出正确结果：1234    则程序提示：YOU BET

要求程序具有用户友好性，可以处理各种非法输入。

2、（30分）实现一个函数，在给定的数组中找出一个子数组，使该子数组各元素的和最大，返回这个和。例如有数组：

#define LEN 10
int a[LEN] = { 31, -41, 59, 26, -53, 58, 97, -93, -23, 84 };

该函数应该返回a[2..6]的和，也就是187。请尽可能设计时间复杂度最优的算法，但要保证算法的正确性和通用性，老师批卷时肯定要换另外一组数据来测试。

编码风格（30分） 请严格遵守教材第9章介绍的编码风格。基本上，缩进空白和注释10分（空白和注释应该出现在必要的地方，不是说越多越好），标识符命名10分（起有意义的英文标识符，如果有缩写要缩得合理），函数和代码结构设计10分（函数分割和封装得是否合理，提供的接口是否合理）。


